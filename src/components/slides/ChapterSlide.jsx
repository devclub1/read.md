const ChapterSlide = ({ title }) => {
  return (
    <div className="flex items-center justify-center h-full text-center">
      <h1 className="text-4xl font-bold">{title}</h1>
    </div>
  );
};

export default ChapterSlide; 